# AI FDE 转型学习进度

## Day 0 — 2026-08-27（序）

今天完成：

- 明确目标为日本普通公司的 AI FDE / Solution Engineer 及相近客户交付岗位。
- 确定每周投入约 10 小时，第 3 个月开始投递，第 6 个月形成较完整的转型证据。
- 确定技术主线：Python → 简单 API → LLM API → RAG → 小型 PoC → 客户交付材料。

当前环境：

- Windows
- Python 3.11.15 可用，后续学习项目固定使用此版本。
- 系统默认 Python 3.14.5 暂不用于学习项目。
- Git 可用。
- PyCharm 已配置为学习用 IDE。

## Day 1 — 2026-08-28

状态：已完成

任务单：见 `Week1/day01/README.md`。

完成后填写：

- 实际用时：没记
- 是否通过自动检查：通过
- 今天独立写出的内容：大部分都是独立写出
- 最不理解的一个点：tuple这个概念 为什么python没有switch case这个语法
- 明天需要复习的内容：tuple

## Day 2 — 2026-08-29

状态：已完成（2026-08-30 验收通过）

任务单：见 `Week1/day02/README.md`。

今天的重点：

- 理解 `tuple[str, str] | None` 与 tuple 解包。
- 用标签映射减少 Day 1 的重复 `if / elif`。
- 支持日文全角冒号，并处理空内容。
- 为输入文件不存在或编码错误提供简短错误信息和退出码。

完成情况：

- 实际用时：未记录。
- 是否通过自动检查：通过；实际生成的 UTF-8 JSON 也已验证。
- 今天完成的内容：tuple 解包、标签映射、半角/全角冒号处理、未知行处理、文件读取异常和退出码。
- 重点订正：tuple 和 list 都有顺序；tuple 创建后不能增删或替换元素，list 可以动态修改；dict 才是键值对。
- 遇到的问题：`str.find()` 找不到时返回 `-1`，但 `-1` 在条件判断中仍为真。
- 明天需要复习的内容：`try/except`、`None` 判断，以及 tuple/list 的使用场景。

## Day 3 — 2026-08-30

状态：已完成（2026-08-31 验收通过）

任务单：见 `Week1/day03/README.md`。

今天的重点：

- 用 `dataclass` 为分类结果提供具名字段，并理解 `frozen=True`。
- 明确使用 `is None` 处理无结果场景。
- 使用 pytest 编写和运行自动测试。
- 为缺少功能或验收条件的输入生成业务校验错误。

完成情况：

- 实际用时：未记录。
- pytest：`12 passed`。
- 今天完成的内容：`RequirementItem` dataclass、具名字段解析、必要分类校验、pytest 测试和 CLI JSON 输出。
- 边界测试：同时缺少两个必要分类；已知标签没有合法冒号。
- 重点订正：tuple 适合少量、固定顺序且不可变的数据；`None` 用于表示没有值，空 list 表示存在但没有元素的集合。
- 遇到的问题：字典循环对象选择、`if/elif` 导致校验漏报，以及缺少分隔符时的异常处理。
- 明天需要复习的内容：根据 Day 4 新任务继续巩固 dataclass、异常处理和测试设计。

## Day 4 — 2026-08-31

状态：已完成（原计划 2026-08-31，2026-09-01 验收通过）

任务单：见 `Week1/day04/README.md`。

今天的重点：

- 理解 module、package、`__init__.py` 和相对导入。
- 把数据模型、解析、校验与 CLI 拆分到不同模块。
- 统一通过 `day04` package 导入公共接口。
- 使用 12 项回归测试保证重构前后行为一致。

完成情况：

- 实际用时：记录约 10 分钟；原计划日未学习，次日继续完成，没有叠加补课。
- pytest：`12 passed`。
- CLI：通过 `python -m Week1.day04.cli` 正常运行，退出码为 `0`。
- 今天完成的内容：把数据模型、解析、业务校验和 CLI 拆分为独立模块，并通过 `__init__.py` 提供统一公共接口。
- 重点理解：package 是组织多个 module 的导入命名空间；`__init__.py` 是 package 的公共入口；`-m` 在 package 上下文中运行模块。
- 验收依据：重构后相同的 12 项输入输出断言继续通过，说明已覆盖行为没有改变。
- 明天需要复习的内容：module、package、命名空间、相对导入和 `python -m`。

## Day 5 — 2026-09-02

状态：已完成（原计划 2026-09-02，2026-09-03 验收通过）

任务单：见 `Week1/day05/README.md`。

今天的重点：

- 使用 `argparse` 定义位置参数、可选参数和自动帮助。
- 使用 logging 区分 DEBUG、INFO、WARNING、ERROR。
- 保持 Day 4 的 package 结构与 12 项回归行为。
- 为文件错误和业务校验失败补充 CLI 测试。

完成情况：

- 实际用时：2 小时以上。
- pytest：`17 passed`。
- CLI：`--help` 返回 `0`；`--verbose` 能输出 DEBUG 日志。
- 三种业务退出码：正常输入 `0`、文件读取失败 `1`、业务校验失败 `2`。
- 异常验收：文件不存在和非 UTF-8 输入均被捕获，不输出 traceback，也不创建结果文件。
- 输出验收：正常输入和业务校验失败均按要求生成 UTF-8 JSON；日文不被转义。
- 今天完成的内容：使用 `argparse` 定义位置参数和可选参数，使用分级日志替代 `print()`，并补充 CLI 文件与业务错误测试。
- 重点订正：捕获多种异常要写成异常类型 tuple，例如 `except (FileNotFoundError, UnicodeDecodeError)`；使用 `or` 只会计算并返回其中一个异常类。
- 明天需要复习的内容：`argparse`、日志级别、异常 tuple、module/package/namespace，以及 `python -m` 的 package 上下文。

## Day 6 — 2026-09-03

状态：已完成（2026-09-04 验收通过）

任务单：见 `Week1/day06/README.md`。

今天的重点：

- 理解类型标注与 Pydantic 运行时校验的区别。
- 使用 `BaseModel` 定义稳定的输入输出 Schema。
- 使用 `Field(default_factory=list)` 创建相互独立的 list。
- 使用 `ValidationError` 测试错误数据，并用 `model_dump_json()` 输出 JSON。
- 保留 Day 5 的 argparse、logging、异常处理和三种退出码。

完成情况：

- 实际用时：2～3 小时；跨日完成。
- pytest：Day 5 与 Day 6 回归测试合计 `25 passed`，其中 Day 6 为 `8 passed`。
- Pydantic 校验：字段类型错误和 Schema 未定义的未知字段都会产生 `ValidationError`；额外确认缺少无默认值的必填字段也会报错。
- 输出 Schema：顶层稳定包含 `schema_version`、`requirements` 和 `validation_errors`，日文以 UTF-8 可读形式输出。
- CLI：正常输入、文件读取失败和业务校验失败的退出码保持为 `0 / 1 / 2`；文件错误无 traceback 且不创建结果文件。
- 今天完成的内容：使用 `BaseModel`、`ConfigDict(extra="forbid")`、`Field(default_factory=list)` 和 `Literal` 定义模型，通过 `model_validate()` 绑定并校验 parser 数据，并用 `model_dump_json()` 序列化输出。
- 重点理解：Schema 描述数据结构契约，`validate_result()` 检查业务规则；Python 类型标注本身不会自动执行运行时校验，Pydantic 在外部数据进入程序时补充运行时校验。
- 今天最不理解的点：Python 的继承语法以及函数返回类型标注。
- 明天需要复习的内容：`class Child(Parent)`、`-> AnalysisOutput`、必填字段与默认字段，以及 Schema 校验和业务校验的边界。

## Day 7 — 2026-09-04

状态：已完成（原计划 2026-09-04，2026-09-05 验收通过）

任务单：见 `Week1/day07/README.md`。

今天的唯一主主题：使用 `requests` 调用 JSON HTTP API，并用 Pydantic 校验响应。

复习点（不单独扩展为第二个主题）：

- 通过 `class RequirementApiError(RuntimeError)` 理解 Python 继承语法。
- 通过 `-> RequirementData` 理解函数返回类型标注。
- 继续区分 Python 类型标注与 Pydantic 运行时校验。

完成情况：

- 实际用时：约 2～3 小时；跨日完成。
- pytest：Day 6 与 Day 7 合计 `15 passed`，其中 Day 7 为 `7 passed`。
- HTTP 成功路径：`requests.get()` 正确接收 URL 和调用者传入的非默认 timeout，JSON 响应经过 Pydantic 校验后返回 `RequirementData`。
- 错误路径：超时、HTTP 4xx/5xx、非法 JSON 和 Schema 错误均转换为 `RequirementApiError`，并通过 `raise ... from exc` 保留原始原因。
- 本地手动验收：本机 HTTP 服务器的正常 JSON 返回成功；404 被转换为 `RequirementApiError`，底层原因为 `HTTPError`。
- 今天完成的内容：理解 `requests.get()`、`Response.raise_for_status()`、`Response.json()`、timeout、自定义异常继承和返回类型标注，并首次使用 `FakeResponse` 与 `monkeypatch` 隔离外部网络依赖。
- 重点订正：`response.json()` 是把 JSON body 解析成 Python 对象；`raise_for_status()` 是检查 HTTP 状态而不是模拟响应；测试参数应使用非默认值，才能发现生产代码把参数写死的问题。
- 当前弱项：Python 基础语法和间接调用链的阅读速度较慢；对单元测试中的被测对象、外部依赖、测试替身以及 Arrange-Act-Assert 结构还不熟练。
- 明天需要复习的内容：单元测试调用链、`FakeResponse`/`monkeypatch` 的职责、默认参数测试，以及 Schema 错误与 JSON 解析错误的区别。

## Day 8 — 2026-09-05

状态：已完成（2026-09-06 验收通过）

任务单：见 `day08/README.md`。

今天的唯一主主题：通过 HTTP CLI 集成边界，掌握 pytest 的调用链、测试替身、`monkeypatch` 和 Arrange–Act–Assert。

继承与复习：

- 复用 Day 7 的 `fetch_requirement()`，不重写已经验收的 HTTP 逻辑。
- 区分 `FakeResponse`、假的函数和 `monkeypatch` 各自替代的对象。
- 分别测试默认 timeout 与非默认 timeout。
- 通过 `RequirementApiError.__cause__` 区分 JSON 解析错误与 Schema 错误。
- CLI 失败时同时验证退出码和“不生成输出文件”的业务约束。

本次验收（2026-09-06）：

- 实际用时：约 3 小时，以学习者笔记自报为准；不以开始/结束时间差或 Cmder 日志跨度计算。
- Day 6～8 回归：`20 passed`，其中 Day 8 为 `5 passed`；测试含真实断言，默认值、非默认 timeout、JSON/Schema 原因及错误时文件不存在均有覆盖。
- 本地真实 HTTP CLI：帮助退出码 `0`；成功退出码 `0`、INFO 日志、UTF-8 日文和完整输出结构正确；404 退出码 `1`、ERROR 日志、无 traceback、不创建文件；缺少参数由 argparse 返回 `2`。
- Cmder 可见的 8 轮相关 pytest 输出：`18 → 18 → 18 → 19 → 19 → 19 → 19 → 20 passed`（每轮共 20 项）。这是日志可见范围，不代表完整学习尝试次数。
- 该段旧日志没有 `CMD_META`，无法可靠还原每轮开始/结束时间和进程退出码。日志仅用于学习过程分析，不用于计算实际用时。
- 已解决的问题：实例方法误用类调用；把输出路径传成 timeout；Fake 返回类型与被替换函数不一致；JSON 字符串误用 `model_validate()`；错误的 patch 位置及异常实例与类的比较。重复错误与修正已追加到当天错题本。
- 复查结果：学习后复盘和日语说明已填写；重新运行 Day 6～8 仍为 `20 passed`。代码与测试未改变，沿用同日已完成的真实 CLI 验证证据。
- 主要收获：能区分测试对象与依赖，使用 Fake 和 monkeypatch 隔离 HTTP 调用；能通过非默认值验证参数转发，通过 `__cause__` 验证异常原因，并检查输出文件副作用。
- 理解订正：单元测试可以调用多层代码，本题替换直接依赖是为了聚焦该层职责；默认值测试应确认具体默认值 `5.0`；JSON 解码是字符串到 Python 对象，Schema 校验是对象到符合契约的模型。补充说明已追加笔记，原答案保留。
- 下次复习：优先复习 `JSON 字符串 → dict/list → Pydantic 模型` 与 `JSONDecodeError / ValidationError`；再用小例子复习 `args` 与 `self.args`、参数展开和实例方法。接下来可按路线进入 Day 9 FastAPI 最小 API。

## Day 9 — 2026-09-06

状态：已完成（2026-09-07 验收；本次学习跨午夜）

任务单：见 `day09/README.md`。

唯一主主题：用 FastAPI 把已有需求解析器提供为 HTTP API。

- 接口：GET `/health` 与 POST `/analyze-requirement`；复用 Week1 Day 6 的 parser、业务校验、AnalysisOutput 和 build_output。
- 最小知识：应用与服务运行器、路由装饰器、Pydantic 请求 body、响应模型、200/422、TestClient。
- 复习：JSON 字符串 → Python 对象 → 模型；请求 Schema 错误与业务缺项的区别。暂不引入异步、依赖注入、数据库、LLM 和部署。
- 四项 TODO：两个接口处理函数、两项请求响应测试；核心答案保留给学习者实现。
- 新增依赖：FastAPI、Uvicorn、HTTPX，使用根目录 Python 3.11 虚拟环境并记录在 requirements-dev.txt。
- 初始预期：Day 9 为 `5 passed, 5 failed`（明确的练习失败）；Day 6～9 合计 `25 passed, 5 failed`。全部完成目标为 `30 passed`，另须真实 HTTP 验证与复盘。
- 脚手架检查：已实测合并回归 `25 passed, 5 failed`，失败均为 TODO；`pip check` 通过。Uvicorn 能启动、OpenAPI 两个接口及模型正确、`/docs` HTML 可获取，真实 HTTP 错误请求返回 422。临时验证服务已停止，正常接口逻辑仍待学习者实现。
- 最终实现：学习者完成健康检查、需求分析路由和两项请求响应测试；复用旧解析与输出逻辑，无需重写。
- 最终验收：Day 9 全部 10 项通过，Day 6～9 独立回归 `30 passed`；`pip check` 通过，两条已知间接依赖弃用警告保留。真实 HTTP 验证 health、docs HTML、OpenAPI、日文 UTF-8 完整输出、类型错误/损坏 JSON 的 422、业务缺项的 200、错误方法的 405，均符合契约；临时目录未生成输出文件、正常路径无 traceback，临时验收服务已停止。
- 实际用时：学习者自报 `1h左右`，不根据笔记起止时间或 Cmder 日志跨度推算。
- 提交前检查：全仓回归 `71 passed`，Uvicorn CLI 帮助正常退出；UTF-8、JSON 样例及敏感信息扫描通过。
- 学习过程：Cmder 完整结果依次为 `8 passed/2 failed → 28 passed/2 failed → 8 passed/2 failed → 9 passed/1 failed → 10 passed → 30 passed`；主要是自写测试的 JSON 层级、版本类型、字段尾随空格错误，已修正并整理到错题本。终端重绘中的无结果命令不计作额外执行。
- 主要收获：把既有纯函数接到 FastAPI 路由，通过 Pydantic 声明请求/响应契约，使用 TestClient 和真实 HTTP 验证不同输入与两类校验结果。
- 复盘订正：Requests 是客户端，FastAPI 是服务端；TestClient 仍返回 Response，不用 FakeResponse 的原因是没有外部依赖；路由装饰器不是注释，HTTP 方法和路径共同决定路由。原答案保留，验收者补充已写入笔记，需下次自行复述确认。
- 教学调整：结束时间放在笔记最后一行；后续同一主题下适度增加一项独立实现与验证，继续按自报用时和反馈调节。已写入 AGENTS.md，不给 Day 9 追加验收要求。
- 下次复习与方向：先用一个例子复述客户端/服务端、JSON 解码/请求模型校验/业务校验及嵌套响应结构；Day 10 可围绕 API 错误处理，增加一个明确的业务失败场景、状态码/错误结构与对应测试，继续复用已有解析器。新日具体范围开始时再确定。

## Day 10 — 2026-09-07

状态：已完成（2026-09-08 最终验收，复盘订正与澄清已确认）

任务单：见 `day10/README.md`。

唯一主主题：API 错误处理，用 HTTPException 明确表达拒绝原因。

- 新增 strict 布尔查询参数，默认 False 保留正常分析；严格模式下业务缺项返回 400/code/errors，JSON/请求模型/query 错误继续使用框架 422。
- 按 Day 9 反馈增加同主题独立功能：原始 text 最多 2000 个字符，超长在解析前返回 413，仅含长度信息；测试边界值和执行优先级。不把此检查误当成生产级 HTTP body 限制。
- 复用 Day 6 解析/业务校验/输出模型和 Day 9 请求模型，不修改历史日；只建立独立 Day 10 应用。不新增依赖。
- 五项 TODO：两个错误策略函数，三项自写测试（单缺项、空白输入的两种模式、错误后下一次成功请求）。核心代码保留给学习者。
- 复习：客户端/服务端、TestClient 的 Response、嵌套 JSON 层级和字段类型；先读非测试代码的调用链再实现。
- 初始预期 Day 10 `11 passed, 6 failed`，全部完成目标 `17 passed`；Day 6～10 目标 `47 passed`。脚手架暂不执行新增限制，预期失败有明确说明，不能标记为完成。
- 脚手架实测：Day 10 `11 passed, 6 failed`，Day 6～10 `41 passed, 6 failed`；历史测试 `71 passed`，`pip check` 通过。真实 HTTP 的 health、docs HTML、OpenAPI/query 默认值、日文 200、请求/query 错误 422 已验证；400/413 仍待 TODO，未误报完成。临时验证目录无输出文件，临时服务已停止。
- 实际用时：学习者自报 `1.5h`，不依据 `2155`～`0003` 或日志时间跨度计算；结束时间 `0003` 保留在笔记最后一行。
- 当前验收：Day 10 `17 passed`，Day 6～10 `47 passed`，全仓 `88 passed`，`pip check` 通过。两条已知间接依赖弃用警告保留，不影响结果。
- 真实 HTTP：health、docs HTML、OpenAPI/query 默认值、默认业务缺项 200、严格缺项 400、2000 字符允许、2001/2017 字符 413、字段/JSON/query 错误 422、错误方法 405、错误后的成功请求均通过；完整日文 UTF-8 输出正确、临时目录无输出文件、访问日志无 traceback。临时验收服务已停止，源码未发现外部 API 调用。
- 学习过程：本日相关 Cmder 中有 13 轮完整 pytest 结果；Day 10 失败数前 12 轮为 `4 → 4 → 4 → 4 → 3 → 2 → 2 → 2 → 2 → 2 → 1 → 0`，随后合并回归 `47 passed`。前两轮的三项测试 TODO 失败是未实现占位，不视为三项独立概念错误。日志有 200/400/413/422 与服务关闭证据，不据此推算时长。
- 已修正错题：detail 重复嵌套；JSON body 误用 set；混用成功响应和错误响应的字段；字典遍历拿到 key 后与 list 比较；把 Any 当索引；字段拼写和 code 尾随空格。错误写法、原因及最小示例已追加笔记，当前测试没有这些错误。
- 复盘待确认：strict 来自 query，当前请求模型拒绝 body 中的 strict；return 结束当前函数而调用者可继续，raise 向外传播；模式按业务用途选择而非测试/生产环境；validate_result 只产生缺项列表，由 check_business_errors 决定是否抛 400，默认模式即使缺少必填业务项也返回 200。验收者解释不代替学习者自行复述，暂不标记已完成或执行完成提交。
- 教学反馈：学习量“合适”，后续保持约本日的独立实现和验证量，不继续加码。长度题应明确问比较运算符和边界测试目的，相关提问规则已写入 AGENTS.md。
- 下一步：只订正笔记第 2～5 题，不追加代码任务；确认后更新完成状态并按规则提交、推送、核对远端。后续再按路线继续 API/LLM 主线，不在本次验收扩展新库。
- 第二次复查（2026-09-08）：全仓仍为 `88 passed`。学习者已补充订正，第 3 题正确区分辅助函数 return 与 raise，第 4 题已改为按用途选择模式；第 2 题已明确 query 和 body 中 strict 会得到 422，补充机制为请求模型的 extra="forbid"。第 5 题仍未说明 strict=False 时即使缺少功能/验收条件也返回 200，不是只有“缺的不是限制项”才不拒绝。待这一具体条件确认后完成上传，不要求重写已通过的题目。
- 本次补充答疑：实测 requirements 为 dict，分类值为 list，类似 Java 的 Map<String, List<String>>；可以遍历 .values() 检查空列表，但需另外验证字段集合，不能与整个 dict 比较的覆盖范围混为一谈。已在笔记保留问题并追加说明。相关 Cmder 日志大小和修改时间未变，沿用前次过程分析，不新增学习轮数或重估时长。
- 最终确认（2026-09-08）：学习者在对话中澄清“我说反了 我以为是strict=true”。此前 400/detail 的回答对应严格模式，而问题是默认模式；已据此结束该项复盘确认，不再要求重复抄写。对照结论保留在笔记：缺项时 strict=False 返回 200/validation_errors，strict=True 返回 400/detail/errors。上方“待确认/下一步订正”均为当时检查过程的历史记录，不是当前未完成项。
- 最终复测：全仓 `88 passed`，`pip check` 通过；实现无功能变化，沿用同日已完成的真实 HTTP 与日志验收证据。只清理测试文件末尾多余空行以通过提交差异检查，保留学习者实现和答案。
- 主要收获与次日复习：能实现查询参数控制的业务错误策略、字符数上限及拒绝顺序，用真实断言验证两种模式与连续请求；次日简短复习 dict/list 层级、.values() 与完整字段检查、strict 的双条件，再按路线继续 API/LLM 主线。学习量保持本日水平，不追加惩罚性练习。

## Day 11 — 2026-09-08

状态：已完成本日收尾（2026-09-09 代码验收通过；用户选择参考答案复盘，概念理解待巩固）

任务单：见 `day11/README.md`。

唯一主主题：可替换分析服务与 FastAPI 依赖注入，为后续 LLM 集成建立可测试的调用边界。

- 最小知识集：provider 函数、Depends、分析器对象的方法、dependency_overrides 字典，以及 monkeypatch.setitem 的自动恢复。先读非测试调用链，再看替换测试。
- 复用：规则分析器包装 Day 6 既有解析/校验/输出逻辑；HTTP 层沿用 Day 9 请求模型与 Day 10 的长度/严格模式检查；不修改历史日，不新增依赖。
- 同主题独立功能：只将分析器的已知临时不可用异常转换为安全的 503；默认/严格的业务缺项仍是 200/400，长度/请求错误仍是 413/422。
- 五项 TODO：路由使用注入对象、异常转换、严格模式不可用测试、超长不调用分析测试、严格模式使用注入结果的业务错误测试。核心实现保留给学习者。
- 原有 API 行为在初始脚手架中继续可用，但尚未使用注入结果或处理 503；初始预期 Day 11 `7 passed, 9 failed`，Day 10～11 `24 passed, 9 failed`。完成目标分别为 `16 passed` 和 `33 passed`，全仓目标 `104 passed`。
- 提供独立本机失败演示，用于后续真实 HTTP 503 验证；不接真实 LLM、不使用密钥、不调用外部 API。区分“provider 已执行”和“analyze 没被调用”，避免把长度检查误说成阻止依赖创建。
- 脚手架实测（2026-09-08）：Day 11 `7 passed, 9 failed`，Day 10～11 `24 passed, 9 failed`，全仓 `95 passed, 9 failed`；失败全部为本日预期练习，历史 88 项保持通过，`pip check` 通过。两个独立本机进程的正常/失败 provider 演示均可启动；旧状态码、日文响应、docs HTML/OpenAPI 和无输出文件检查通过。失败演示仍返回 200，503 待实现；两个临时验收进程已停止，未标记学习完成。
- 复习仅包含 dict/list 层级和 strict 两种模式；学习量保持 Day 10 水平，参考 90～120 分钟。实际用时待学习者自报，结束时间位于笔记最后一行，不以日志估算。
- 最终验收（2026-09-09）：学习者完成路由委托、503 转换及三项测试；Day 10～11 合计 33 passed（包含本日全部 16 项），全仓 104 passed，pip check 通过。真实 HTTP 验证 200/400/413/422/405/503、固定错误结构、2000/2001 边界、失败服务超长优先拒绝、日文 UTF-8、docs/OpenAPI、独立进程隔离、无输出文件和无 traceback，验收服务已停止。上方 TODO/预期失败为历史过程，不是当前故障。
- 复盘方式：学习者明确表示很多概念仍不理解，并要求直接给答案、结束当天。助手保留全部原答案，追加十题参考答案、日语参考说明和错题本；不再追问，不把代答记录为学习者已独立掌握。代码成果验收通过与概念待巩固分别记录。
- 实际用时：自报 2h；结束时间 2115 保留为学习记录最后一行，不用日志或起止时间推算。
- 学习过程：Cmder 可见 analyze 缺 text 的重复报错，后续有 16 passed、33 passed；不据有限日志推断完整尝试次数。助手本轮先检查到 5 passed / 11 failed，再确认修正后全部通过；主要问题是多余分析调用、503 字段协议、函数与调用结果、Response 与分析器 calls 混淆，已整理到当天错题本。
- 教学调整：本日“偏难，完全消化不了”，下次先用 test_override_scope_restores_real_service 一个案例区分请求、提供对象的函数、分析器和响应，不默认具备 Spring 经验，不引入新库或叠加补课。有关逐段解释和用户选择参考答案收尾的规则已写入 AGENTS.md。

## Day 12 — 2026-09-10

状态：已完成（2026-09-10 原作业验收通过；用户主动要求的追加复习另行进行）

任务单：见 `day12/README.md`。

- 本日已确认的理解：学习者能说明 RuleBasedAnalyzer.analyze 接收 str、返回 AnalysisOutput，主要字段为 requirements/validation_errors，RequirementData 内部有多个 list；通过三类关系图确认。只记录这一层已理解，不推断依赖替换也已掌握。
- 唯一主题：用一个恢复测试捋清提供对象的函数、替身对象、分析结果和 HTTP Response；复用 Day 11 已完成的 API，不重写路由，不引入新库或新业务主题。
- 作业仅两项：实现 FixedAnalyzer.analyze 的记录和固定返回；完成同一替身接收两次不同请求的测试，验证完整响应、原文与顺序。构造函数、具名提供函数、替换与恢复框架已经提供；先逐段讲解，再动 TODO，不提前堆 lambda 或复杂 fixture。
- 复盘减为三题，可在对话中说明；不补昨天十题，不增加日语作文。参考约 90 分钟，以实际理解和自报用时调整，不凑量。开始时间与实际用时待学习者填写，结束时间在笔记最后一行。
- 初始预期本日 2 passed / 3 failed；TODO 1 完成后目标 4 passed / 1 failed；全部完成目标本日 5 passed、Day 11～12 为 21 passed、全仓 109 passed。当前占位失败不视为项目故障，待验证脚手架结果后记录实测。
- 脚手架实测（2026-09-10）：本日 2 passed / 3 failed，全仓 106 passed / 3 failed，失败全部对应本日两个 TODO；历史 104 项通过。文件 UTF-8、敏感信息扫描和差异空白检查通过。仅建立本地作业，没有修改历史代码、安装新依赖、填写学习时长或标记完成；待学习者完成后按规则验收并上传。
- 最终验收（2026-09-10）：原作业 Day 12 全部 5 项通过，Day 11～12 为 21 passed，全仓 109 passed，pip check 通过。实际代码按原文追加调用记录、返回预设模型；自写测试发两次不同请求，分别检查完整结果/200，再检查调用记录的内容与顺序。临时目录中的独立观察脚本验证正常/超长/恢复、固定错误结构、对象提供次数与分析次数、UTF-8、退出码 0、无输出文件及无 traceback；无新增对外服务。
- 复盘：能够区分 FixedAnalyzer、AnalysisOutput、Response，理解连续记录与离开 with 后恢复。补准表达：dependency_overrides 不改写 get_analyzer 函数本身，仅影响 FastAPI 解析 Depends 时的函数选择；不因此否定原作业已经掌握的部分。原答案保留，补充写入笔记。
- 实际用时：学习者自报 20min；结束时间 2248 保留为笔记最后一行，不以 2229～2248 差值或日志估算。Cmder 四轮可见完整结果为 2 passed/3 failed → 4 passed/1 failed → 5 passed → 21 passed；前两轮是未完成占位，不新增未经证实的概念错题。
- 教学反馈：今天偏少、没有太迷糊。将概念难度与代码量分开调整，不因前一天吃力持续压低负荷；已更新 AGENTS.md。用户主动要求追加，提供 review_day11.md/review_flow.py，先观察覆盖边界，再观察取得对象与分析调用的区别；可选新增“成功后超长但保留旧调用记录”的测试。
- 追加复习状态：材料已由助手验证，尚未确认学习者完成，追加时长未记录；不重开原作业、不追补昨天十题。下一步从观察脚本编号 1/2 开始，再看编号 3；理解后可写可选测试，不急着进入新库或 LLM。
