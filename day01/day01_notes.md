# Day 1 学习记录

## 读代码的五个问题

1. `main()` 从哪里开始运行？
if __name__ == "__main__":
    main()→这里开始
2. `Path` 表示什么？
把硬盘上的文件变成程序可以操作的对象
3. `parse_requirement()` 收到的参数是什么类型？
str
4. 返回值为什么是 `dict[str, list[str]]`？
为了方便整理吧 key值是機能　受入条件... value的list是实际的内容
5. `json.dump()` 的作用是什么？
相当于写方法 outputstream.write?
## 完成后的复盘

1. 实际用时：
我没计时 中间休息了很久 不是很专心
2. `list` 和 `dict` 分别解决什么问题？
list就是列表 dict是hashmap 我觉得就是线性存储和键值对存储的区别
3. 为什么不能把结果写死？
那程序就没法复用了
4. 第一个错误以及修复方法：
返回的tuple如果是none的话 没有做检查处理 还在取里面的值 修复方法是先检查是不是none 是的话直接continue掉
5. 日语客户说明（3～5 句）：
開発テーマの要件定義資料をフォーマットして、該当プログラムに入力すると、機能、受入条件などそれぞれをリスト化して、見やすいように整理できるプログラムです。
プログラム本体がday01_requirement_parser.py、利用する前に、フォーマット済み要件定義資料を同フォルダに格納し、プログラムを開始する。
プログラム終了したら、同フォルダにアウトプットしたresult.jsonは整理完了の要件定義資料です。
プログラムにエラーが発生したら、提示した文言より、入力資料のフォーマットをご確認ください。