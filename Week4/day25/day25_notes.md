# Day25 学习记录 — API 报告边界校验

## 学习开始

- 开始时间：1703
- 环境确认：

## 学习目标（无需提前作答）

- 区分 HTTP 状态、JSON 解码和报告结构这三道检查。
- 解释字段校验与汇总一致性校验各自覆盖什么。
- 用一个错误汇总的测试证明页面不会显示误导人的数字。

## 执行记录（学习后填写）

- 初始测试结果：
- 完成后当天测试结果：
- Chrome 中 A / A+B 的页面结果：
- 遇到的主要错误及修正：

## 学习后复盘（完成实现后用自己的话回答）

1. 在 `code/ui_client.py` 的 `submit_batch` 中，服务器先返回 HTTP 500、正文为 `not-json`；再考虑另一次请求返回 HTTP 200、正文也为 `not-json`。两次分别显示哪条确切消息？按“500 / 200”两行回答，并说明先后检查顺序。
   - 回答：500 DemoError(f"接口请求失败（HTTP {exc.response.status_code}）")
   - 200 DemoError("接口未返回合法 JSON")
2. 在 `tests/test_response_validation.py` 的 `valid_body()` 中，A 的 `ok=True`、B 的 `ok=False`，两行 `attempts` 都是 1，汇总为 `2/1/1/2/0`。如果只把 `summary.requests` 改为 3，Pydantic 字段类型检查能否单独发现？`validate_report` 应显示哪条确切消息？按“类型检查 / 函数结果”两项回答。
   - 回答：发现不了 类型是没问题的 但是因为有值的对比判断 所以返回  ("接口返回结构不符合预期")
3. `ResultResponse.raw` 声明为 `str | None`，没有默认值。若 B 的结果含 `"raw": null`，以及若完全没有 `raw` 这个 key，这两种输入各应通过还是失败？按“null / 缺 key”两项回答。
   - 回答：null 通过  缺key 失败

## 错题本与验收反馈

### 助手首次检查（2026-09-25；保留以上原答）

- 本日测试 `104 passed / 7 failed`，`pip check` 通过。七项失败归为两处实现：`ResultResponse` 没有 `failed` 属性，`validate_report` 遍历任务时访问它会抛 `AttributeError`；`RetryResponse.attempts=0`、`SummaryResponse.retries=-1` 仍被模型接受，因为对应字段尚未声明 `Field(ge=...)` 约束。
- 自写 `test_day25_extra.py` 虽通过，但断言 `body == body_copy` 写在 `pytest.raises` 内、且位于会抛异常的调用之后，实际不会执行；应把比较放在 `with` 块外，并用调用前的深拷贝与调用后的输入比较。不能把该项通过当作已验证“不修改输入”。
- 复盘第 3 题正确。第 1 题的执行顺序正确，但题目要求显示的确切消息，应写 `接口请求失败（HTTP 500）` 与 `接口未返回合法 JSON`，而非 `DemoError(...)` 代码表达式。第 2 题正确指出类型检查发现不了汇总矛盾；函数行为应表述为抛出带消息 `接口返回结构不符合预期` 的 `DemoError`，不是返回字符串。
- 测试未全通过，浏览器手动验收和完成提交暂不进行；当前不标记 Day25 完成。学习者执行记录中的空项保留，不推断其实际操作。

### 助手第二次检查（2026-09-25；保留原答与首次反馈）

- 学习者已补齐响应模型的数值下限，原有 111 项测试一度全通过。代码审查发现 `result_fail += model.summary.failed` 使用待校验的汇总值作为“实际失败数”，因此只把 `summary.failed` 从 1 改为 0 时，`validate_report` 错误地接受了含一行 `ok=False` 的报告。助手按 TODO 2 已写明的契约补上一项只改 `failed` 的测试；当前 **111 passed / 1 failed**，失败正是未抛 `DemoError`。此项属于原有要求的测试覆盖缺口，不是新增学习要求。
- 自写 `test_day25_extra.py` 的 `assert body == body_copy` 仍在会抛异常的 `validate_report(body_copy)` 之后、位于 `pytest.raises` 块内，尚未实际运行。应保存调用前的深拷贝，调用原 body，并在 `with` 块外比较调用后的 body 与快照。
- `pip check`、已跟踪文件的 `git diff --check` 通过。Day25 仍未通过代码验收；浏览器手动验收、结束时间、完成提交和上传待后续确认。复盘第 1、2 题的实际消息与抛异常措辞仍按首次反馈补准。

### 最终复查（2026-09-25；原答保留）

- 学习者已按任务单修正失败数统计，并在自写测试中把输入不变断言放到 `pytest.raises` 外；调用前的深拷贝与调用后的输入确实比较。Day25 **112 passed**，pip check 与差异检查通过。测试用例还单独覆盖只改 `summary.failed` 的矛盾。
- Chrome 实机：A 单条显示 1/1/0/1/0 与 `模擬結果：A`；A+B 显示 2/2/0/2/0，日文可读；空白 A、2001 字符 A 显示对应错误且不保留旧结果。API 访问日志只记录两次有效提交。停止 API 后合法输入显示连接错误，无旧成功报告；重启 API 后重新提交恢复 A+B 成功。两个由助手启动的临时服务已关闭。
- 复盘第 3 题正确。第 1 题的正确页面文字由助手补准为 `接口请求失败（HTTP 500）`、`接口未返回合法 JSON`；先检查 HTTP 状态，再解码 JSON。第 2 题正确指出字段类型检查无法发现汇总矛盾；函数实际抛 `DemoError("接口返回结构不符合预期")`，并非返回字符串。这两处补准记录为助手说明，不计为学习者独立写出的确切措辞；不要求重复抄写。
- 学习者自报实际用时 1h、难度中、无待讲解点；空白执行记录不补写为学习者操作，助手验收证据见上。代码、运行与概念复盘已完成；按项目规则提交上传。

## 学习反馈

- 实际用时（排除中断，自报）：1h
- 难度与内容量：中
- 仍需讲解的位置：无

- 结束时间：1934
