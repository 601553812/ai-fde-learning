# Day24 学习记录 — 浏览器 Demo

## 学习开始

- 开始时间：1910
- 环境确认：根 .venv Python 3.11；按 README 第 0 节检查脚手架。

## 学习目标（无需提前作答）

- 说明页面、HTTP 客户端、FastAPI 三者的调用顺序。
- 区分表单输入错误、HTTP 请求错误和报告里的逐任务失败。
- 理解网络调用为何放在 if submitted 内。

## 执行记录（学习后填写）

- 初始测试：
- 完成后本日测试：
- A 单条 / A+B 的显示与汇总：
- 空 A / 超长 A 时页面与 API 访问记录：
- 停止 API / 重启 API 的结果：
- 日文、错误消息及无旧成功报告检查：

## 学习后复盘

1. code/ui.py 的 main 初次打开时 submitted=False；填写表单后点击一次提交，下一次运行 submitted=True。分别写出这两次运行是否调用 submit_batch。若把调用移到 if 外面，初次打开会发生什么？各用一句话回答。
   - 回答：不调用 调用 会在打开页面没点按钮的时候也post一次默认值
2. tests/test_day24_extra.py 的输入是 HTTP 200，body 只有 B，B 的 result.ok=False、error=auth_error、status_code=403。submit_batch 应返回报告还是抛 DemoError？render_report 显示成功还是失败警告？再把整个 HTTP 状态改成 500、正文改成 not-json，应显示哪条确切错误消息？按这三项回答。
   - 回答：返回报告 失败  DemoError(f"接口请求失败（HTTP {exc.response.status_code}）
3. code/ui.py 收到 text_a=" \n "、text_b="確認"、max_attempts=1，先执行 build_payload。应显示什么消息、是否执行 submit_batch？如果错误实现先发 POST 再检查空 A，仅断言页面错误消息能发现吗？补写还应观察的调用记录或 API 访问记录。
   - 回答：需求 A 不能为空  不会 不能  应断言客户端调用记录为空

## 错题本与验收反馈

### 助手检查（2026-09-24；保留以上原答）

- 代码：两个函数已实现；此前 HTTP 客户端的 RequestError 曾误从 requests、urllib3 导入，现已改为 httpx.RequestError。当前 Day24 测试 91 passed，pip check 通过。
- 自写测试最初的 payload 写成 `{"task_id":"B","report":"確認"}`，但真实 API 的请求字段是 `text`。RecordingClient 不校验请求模型，因此测试仍能通过。这是测试覆盖缺口；助手只把该输入字段修成 `text`，保留学习者的报告与一次 POST 断言。修正后测试仍为 91 passed。
- Chrome 实机：独立临时标签初次打开无报告；提交 A 得 1/1/0/1/0，提交 A+B 得 2/2/0/2/0；日文显示正常。空白 A 和超长 A 分别出现相应错误，旧成功报告不再显示；恢复合法输入后页面正常。另用真实关闭端口验证连接错误转换为安全的 DemoError。临时标签已关闭，学习者原标签未修改。
- 第 1 题正确：初次打开不调用，提交后调用；把请求移到 if 外会在初次打开时发送默认值。
- 第 2 题第一项需订正：HTTP 200 即使 B 的 ok=False，submit_batch 仍返回完整报告，不抛 DemoError；render_report 显示失败警告。整个 HTTP 500 时会先由 raise_for_status 抛错，再显示 `接口请求失败（HTTP 500）`，不把 not-json 报成 JSON 错误。原答把 200 的失败任务误当成页面级错误，且第三项只写了不完整的代码表达式。
- 第 3 题的消息和“不执行 submit_batch”正确；最后一项需订正：只断言页面错误消息发现不了错误实现是否先发 POST，还应断言客户端调用记录为空或核对 API 访问记录没有新增 POST。
- 学习者执行记录尚未填写。当前 Chrome 验收没有停止用户正在运行的 API，因此停服/重启的页面实测尚未确认；已有 RequestError 测试覆盖连接错误转换。CMDER_ROOT 未设置，未取得 Cmder 日志，不从缺失日志推断学习过程。实际用时沿用学习者自报 1h，不按 1910～2105 差值计算。
- 代码验收通过，概念复盘第 2、3 题待学习者订正；此时不标记整日完成，也不做完成提交。

### 最终复查（2026-09-24；上述原答与首次反馈保留）

- 学习者订正后，第 2 题已写“返回报告、失败”；HTTP 500 的代码表达式所指方向正确，页面实际显示文字补准为 `接口请求失败（HTTP 500）`。第 3 题已写明只看页面错误发现不了先 POST 的错误实现，应断言客户端调用记录为空。第 1 题维持正确。三题核心概念验收通过；第 2 题的确切页面文字由助手补充，不作为独立写出的答案。
- 再次运行本日 91 passed、pip check 通过；真实关闭端口产生预期的安全 DemoError。AppTest 注入一次连接错误后显示错误且没有旧成功报告，下一次成功提交正常显示。没有停止用户正在运行的 API，故未宣称在 Chrome 中实测停服/重启；独立验证已覆盖错误与恢复路径。
- 自报实际用时 1h、难度中；执行记录空项保持原样，助手验证证据在本节，不补写成学习者操作。当前代码、运行与概念验收完成，按项目规则进行提交上传。

## 学习反馈

- 实际用时（排除中断，自报）：1h
- 难度与内容量：中
- 仍需讲解的位置：无

- 结束时间：2105
