# Day 6 学习记录

## 开始前

- 开始时间：

1. Python 类型标注会不会在运行时自动拒绝错误类型？

2. `dataclass` 和 Pydantic `BaseModel` 的主要职责有什么区别？

3. Pydantic Schema 校验与 `validate_result()` 业务校验有什么区别？

4. 为什么多个模型实例不应该共享同一个默认 list？

## 完成后复盘

- 结束时间：
- 实际用时：

1. pytest 最终结果：

2. `RequirementData` 包含哪些字段？

3. 哪两种错误数据会产生 `ValidationError`？

4. `model_dump()` 和 `model_dump_json()` 分别返回什么类型？

5. 正常输入、文件错误、业务校验失败的退出码：

6. 今天完成了哪些 TODO？

7. 今天最不理解的一个点：

8. 明天需要复习的内容：

9. 日语说明（3～5 句，向使用者解释输出 Schema 与校验错误）：
