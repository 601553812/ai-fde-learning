# Day 2 学习记录

## 开始前

1. 本题中 tuple 的两个位置分别保存什么？
分类 和 实际内容
2. tuple 与 list 最重要的区别是什么？为什么这里返回 tuple，而各分类的数据用 list？
tuple 是按顺序保存的一组固定数据 list 用于保存数量可变化的多条内容。
3. 为什么 `classify_line()` 的返回类型中包含 `None`？调用方必须做什么？
如果读取到空行或者注释就需要返回None 调用方必须进行空检查
4. Java `switch`、Python `match / case` 和本题使用的 `dict` 分别适合什么情况？
switch这种适合 读取单个数据匹配到某个选项时进行的操作
dict则是 一次读取多个数据存储在dict里 根据key值读取数据再分别进行操作
## 完成后复盘

1. 实际用时：
没记录
2. 自动检查是否通过：
通过了
3. 请用自己的话解释 `category, content = classified`：
直接把数据拆开 分成两个变量来读取
4. 今天遇到的第一个错误及修复方法：
line.find(":")返回-1 但是如果if判断的话还是true 这个让我觉得很奇怪
5. 为什么文件不存在时要返回非 0 退出码？
和正常退出做区分
6. 明天需要复习的内容：
try catch写法 null判断写法
7. 日语客户说明（3～5 句，重点说明 Day 2 的改善）：
前バージョンより、インプット資料が存在しない場合かつ資料のデコードにエラー発生する場合に対して、ログ出力形で提示して、障害時点の経緯を明確できるように改善いたしました。
また、処理を共通化して、ソースの可読性を上げし、今後運用保守にも改修やすくなりました。