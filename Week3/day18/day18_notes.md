# Day 18 学习记录 — Prompt 修改与对比

状态：今日学习已结束；本地代码与规则复查通过，未填写项按学习者要求留空，真实对比未完成；日期：2026-09-18。

## 学习开始

- 开始时间：1802
- 环境确认：原项目根目录、根 .venv；先按 README 第 0 节检查脚手架。

## 本日学习目标（无需提前作答）

- 将确定条件和未确认候选区分开；固定输入与评分规则，对比两套 Prompt。
- 识别代码测试、合成错误回放和真实模型输出分别能证明什么。

## 实现后：修改假设

- 我希望避免的具体错误：
- 我的候选规则改变了什么：
- 为什么还要验证 A 和 C：

## 运行与结果（执行后填写）

- 初始脚手架测试：根据cmder日志（助手核对：3 passed、8 failed，均为预期 TODO 失败，退出 1）。
- 实现后本日测试 / 全仓测试：all passed（助手核对日志：11 passed / 173 passed；本轮全仓复测也是 173 passed）。
- D 教学回放：来源为人为构造，不能归因为实际模型失败；实际观察：助手核对结构 True、内容 False，检测到 questions 缺少 3秒/確認，以及 acceptance_criteria 不应有内容，退出 1，符合教学回放预期。
- 请求预览：两套 input 是否相同，基线规则是否保留：助手核对两者均是；追加规则的含义冲突已由学习者修正，见下方再次复查记录。
- 实际模型名 / 调用日期：gemini-3.8-flash / 2026-09-18（Cmder 日志）。

| 案例 | baseline 自动结果 | candidate 自动结果 | change | 人工读 raw 的核查与差异 |
|---|---|---|---|---|
| D：未确认候选条件 | 等待响应时被中断，未取得结果 | 未执行到 | 无法比较 | 没有模型 raw 可核查 |
| A：明确验收条件 | 503 后 SDK 重试等待，被中断 | 未执行到 | 无法比较 | 服务不可用，不能当作内容评测失败 |
| C：只有功能 | 结构 True、内容 True、issues=[] | 等待响应时被中断，未取得结果 | 无法比较 | baseline 只保留 CSV 导出功能，其余四类为空，没有编造条件 |

- 结论（采用 / 暂不采用 / 证据不足）及证据：
- 若 API 失败：错误类型、完成到哪一步；未产生的结果保持待执行：助手核对如上表。三条命令最终均被 Ctrl+C 中断（KeyboardInterrupt）；A 明确记录 503 Service Unavailable，D/C 的中断堆栈停在网络读取，不能仅凭这些记录判断具体延迟原因。

## 学习后复盘（用自己的话，简短回答）

1. 打开 `test_day18_extra.py`，看调用 `classify_change(before, after)` 的测试。输入 before 的 `content_passed=None`、after 的 `content_passed=True`：
   - 这个函数预期返回什么字符串？
   - 假设函数错误地返回了 `"regressed"`，但没有抛异常。如果测试只调用函数、不用 assert 检查返回值，测试会通过还是失败？说明原因。

   回答："improved",会通过 因为没有断言

2. 这是一道假设题，不需要先成功运行 API。假设 D/A/C 每个案例中 baseline（原 Prompt）和 candidate（修改后的 Prompt）的 `content_passed` 都是 True：
   - 这次结果有没有显示出“原来未通过，修改后通过”的案例？
   - 仅凭这一轮结果，能不能说修改后的 Prompt 以后会更准确？用 1～2 句说明。

   回答：运行失败
         不能 只能判断返回值符不符合要求 没办法判断语义

3. 仍比较同一轮的两套 Prompt。假设 D 的 `content_passed` 从 False 变为 True；但 A 原文明确写了 `受入条件: 3秒以内`，原 Prompt 正确放在 `acceptance_criteria`，修改后的 Prompt 却将它移到 `questions`，导致 A 从通过变为未通过：
   - 你会直接用修改后的 Prompt 替换原 Prompt 吗？说明原因。
   - 接下来你会修改或检查什么？不必写出最终 Prompt。

   回答：不会 需要定位原因
         检查两个prompt的diff

## 错题本与验收反馈

### 助手首次验收记录（2026-09-18，历史；最新状态见再次复查）

- 自动测试：本日 11 项包含在全仓 173 passed 中；保留两条既有依赖弃用警告。请求构造保留完整原文和原指令，比较函数在约定的 bool/None 输入下正确，自写测试有有效断言。临时让比较函数返回错误的 regressed 时，自写测试会失败；临时替换未写入文件。
- 手动检查：帮助、D 请求预览、A/C 历史回放退出 0；D 人为错误回放退出 1；非法 case 退出 2。逐行 JSON、日文内容和完整 input 正常，在空检查目录运行未生成输出文件。本轮没有重发真实 API 请求。
- 待修规则：prompt.py 将“未确认的候选条件”放 unknown，与基线“明确待确认事项放 questions”以及 D 的业务要求冲突。D 已明确要求向负责人确认 3 秒条件，不能按无法分类处理。“没有依据时也放 unknown”还可能引导模型补写说明；没有原文内容的分类应保持空数组。应先修正这段通用分类规则，再做对比；未代写学习者实现。
- 测试覆盖的限制：现有请求测试证明指令被追加、input 被保留，不理解新增规则的业务含义。173 passed 不证明候选规则或模型效果通过验收。
- 小整理：prompt.py 的 turtledemo.sorting_animate.instructions1 导入未使用，可以删除；自写测试 issues 中的 invalid structure 与约定 invalid_structure 拼写不一致，应统一。后者不影响这次比较函数的返回值，不作为比较逻辑错误。
- Cmder 可见本日测试三轮：8 failed / 3 passed → 2 failed / 9 passed → 11 passed；中间两项是 input 的 JSONDecodeError，当前已修正。日志不能证明编辑器内具体修改过程。全仓测试随后 173 passed，D 预览成功，真实调用情况见上表；本轮未发现比 19:51 的预览更新的学习命令记录。
- 复盘：第 1 题正确，能说明错误返回值在没有断言时不会被测试发现；第 3 题正确，知道不能直接采用，应检查两套 Prompt 的差异。第 2 题原答“运行失败”如实描述了执行情况，但尚未回答假设问题；原题“运行后”的措辞容易误导，现已改为无需 API 成功的假设题，不将此记作学习者错题。
- 脚手架问题：应用没有自行重试，但 SDK 内部会重试；旧 README 的“无自动重试”已更正。请求进度提示、明确超时和重试限制仍待助手改进；不能把服务等待计为学习者作业错误，也不能编造未返回的比较结果。
- 当前结论：尚未完成最终验收；保留真实调用中断的证据，不要求现在反复重试。先修候选规则、补第 2 题假设回答；真实对比待调用问题处理后继续。未提交或推送未完成作业。
- 用时与反馈：沿用学习者自报 2h、难度中等、结束 2014，不用起止时间或日志跨度替代实际用时。

### 助手再次复查（2026-09-18）

- 候选规则已修正：未确认候选条件归入 questions，明确无原文依据的分类为空数组；与原有规则及 D 的业务要求一致。请求预览退出 0，两套 input 相同、原指令完整保留。此前“规则待修”已解决。
- 全仓复测 173 passed（包含 Day18 11 项），两条既有依赖弃用警告不影响结果。本轮没有修改学习者代码，也没有真实调用模型。
- 复盘第 2 题新增回答“不能 只能判断返回值符不符合要求 没办法判断语义”，正确指出了关键词评测的局限。助手补充：题设中新旧都通过，没有出现失败变通过的案例，因此本次没有观察到提升；单轮少量样例也不能支持以后更准确。补充不当作学习者已独立表达的证据，无需重抄本次已澄清内容。
- Cmder 最近三个日志的大小与修改时间均未变化，没有新增真实调用证据。已有 C baseline 成功结果保留，D/A/C 完整对比仍待完成；先前超时与反馈问题仍属助手脚手架待改进项，不要求现在反复重试。
- 当前结论：候选规则和本地代码检查通过，复盘本轮已澄清；整体仍不能标记全部完成或推送完成提交，因为新旧 Prompt 的真实比较与实验结论尚缺证据。未使用导入和测试错误文本拼写保留为非阻塞整理建议，不新增验收门槛。
- 保留学习者原答、实际用时 2h 和结束时间 2014。

### 当日收尾（学习者确认）

- 学习者明确要求剩余项今天留空，到此结束；保留空白，不代填实验结论，也不要求继续调用或补答。
- 已完成成果：候选规则、请求构造、比较函数、自写边界测试及本次复盘澄清；全仓 173 passed。真实对比仍缺完整结果，不能记录为模型效果已验证。
- 后续开始新学习日时，先读取本记录再确定安排；不因今天服务调用受阻而追加补课。超时、重试限制与请求进度提示作为助手后续需处理的脚手架问题，今天不继续修改。
- 今日在此结束，实际用时 2h、结束时间 2014 保留。未做完成提交或推送，避免把未完成的真实验收记为全部通过。

### 后续归档整理（2026-09-24；不修改当日原答与结论）

- 按学习者要求，将 Day18 的已有本地成果作为“当日已结束、真实对比未完成”的记录同步；不是完成提交，不补做真实调用或实验结论。
- 移除 prompt.py 未使用的 turtledemo 导入，并将自写测试 issues 的教学错误名改为任务单约定的 `invalid_structure`；不改变测试判断的 before/after 值。归档时 Day18 11 passed、全仓 501 passed，pip check 通过；离线 CLI 的帮助/预览/回放/非法参数均符合约定，未发起真实模型调用。详细归档验证见根学习进度。

## 学习反馈

- 难度与内容量：中等
- 仍需讲解的位置：无
- 实际用时（排除中断，自行填写）：2h

- 结束时间：2014
