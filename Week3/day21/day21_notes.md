# Day21 学习记录 — 汇总调用结果

## 学习开始

- 开始时间：0858
- 环境确认：原项目根目录，根 .venv Python 3.11，按 README 第 0 节先运行初始测试。

## 学习目标（无需提前作答）

- 区分输入任务数、最终成功/失败数量、底层调用次数与额外重试次数。
- 汇总已完成报告，连续调用之间不残留计数，不改变输入数据。

## 执行记录（做完填写）

- 初始测试：
- 实现后本日 / 全仓测试：助手本轮验收全仓 281 passed，包含 Day21 全部 47 项；不是学习者终端记录。
- mixed 的五项计数及退出码：助手验收 3 / 2 / 1 / 5 / 2，退出 1。
- empty 的五项计数及退出码：助手验收 0 / 0 / 0 / 0 / 0，退出 0。

## 学习后复盘

每题用字段值或 1～2 句回答，不要求重新回答 Day20 题目。

1. 在 run_summary 的 mixed 场景中，先分别统计三份报告（每次只把其中一份放进列表传给 summarize），再单独列三份一起传入时的合计。每行按 requests、succeeded、failed、attempts、retries 顺序写值。这里检查任务数与底层调用数的区别。
   - 回答：1/1/0/1/0
   - 2/1/1/2/1
   - 2/0/2/2/1

   助手逐份说明（按学习者要求拆开列出，以上原答保留）：

   | 单独传入的报告 | requests | succeeded | failed | attempts | retries |
   |---|---:|---:|---:|---:|---:|
   | 第一份：第一次就成功 | 1 | 1 | 0 | 1 | 0 |
   | 第二份：503 后成功 | 1 | 1 | 0 | 2 | 1 |
   | 第三份：503 后 403，最终失败 | 1 | 0 | 1 | 2 | 1 |

   第二份虽然调用两次，但仍只有一个任务，最终成功，所以 succeeded=1、failed=0。第三份也只有一个任务，最终失败，所以 succeeded=0、failed=1。两份中的 attempts=2、retries=1 都正确；不能把两次调用算作两个任务或两次最终失败。

   三份一起传入时，合计另列为：requests=3、succeeded=2、failed=1、attempts=5、retries=2。此表为助手讲解，不代替学习者独立作答证据，不要求重抄已经说明的内容。

2. 在 test_summary 的 test_normal_return_is_not_content_validation 中，唯一报告为 `RetryResult(CallResult(True, "not-json", None, None), 3)`。succeeded 是多少？它是否证明内容准确？说明你用于计数的具体字段。
   - 回答：1 不能 只能证明确实返回了内容 但没有校验内容是否符合要求
3. 在自写 test_second_summary_starts_from_zero 中，第一次是失败/3 次，第二次是成功/1 次。如果错误实现让第二次 summarize 返回的 second["attempts"] 等于 4（残留第一次的 3，再加本次的 1），第二次的完整 dict 断言会通过吗？只写 attempts 的期望与实际值，并说明失败本身是否直接定位了原因。
   - 回答：不会通过 期望1 实际4 只有计数不符合预期 具体原因需要再查

## 错题本与验收反馈

### 助手检查（2026-09-21；保留原答）

- 代码验收通过：summarize 每次从局部计数开始，按最终 ok 计数，累计 attempts，再减去任务数得到额外重试次数；空列表和不修改输入符合契约。自写测试检查两次完整结果，并再次核对 first 未被修改。
- 全仓 281 passed，包含本日全部 47 项；pip check 通过。独立进程检查 mixed/empty/success、帮助、非法参数，退出码分别 1/0/0/0/2；单行 JSON、中文 stderr、无 traceback 和临时工作目录无输出文件均通过。两条既有依赖弃用警告不影响结果。未读取 Cmder 日志，不推断学习者尝试次数。
- 第 1 题需要澄清：题目问三份报告一起传给 summarize 后的一份总统计，应为 requests/succeeded/failed/attempts/retries = 3/2/1/5/2。若分别列每份报告的贡献，则应为 1/1/0/1/0、1/1/0/2/1、1/0/1/2/1。每份报告只算一个任务；中间 503 不单独算最终失败。原答保留，尚未确认是读题方式还是计数概念造成偏差。
- 第 2 题结论正确：succeeded=1，不证明内容准确。补充题目要求的具体字段是 report.result.ok；它表示 complete 正常返回，不保证内容非空、结构合法或语义正确。
- 第 3 题正确：第二次 attempts 期望 1、错误实际 4；失败说明计数不符，仍需调查原因。此前题目中 4 的指代引起疑问，已按对话澄清为 second["attempts"]，无需重答。
- 自报实际用时 1h、难度简单；开始 0858、结束 1012 原样保留，不用时间差替代实际用时。代码已通过，当前仅第 1 题待简短澄清，尚未整日收尾或提交推送。Day18 真实对比继续留空。

### 最终收尾（2026-09-21）

- 学习者选择逐份列出第 1 题，助手已拆分说明每份报告只计一个任务、成功失败看最终结果，并另列合计；学习者表示可以。原答案与助手讲解分别保留，不表述为首次无提示独立作答，无需重抄或追加练习。
- 代码、运行验收与复盘收尾完成。上方待澄清、未收尾是历史过程，当前 Day21 已完成；按项目默认规则提交推送。实际上传结果以 Git 远端核对为准。
- 实际用时仍为自报 1h，难度简单；下次进入 Week4/day22，先用一个现有例子简短巩固任务数与调用次数，再按反馈恢复适量独立练习，不叠加新机制。Day18 真实对比继续留空，不要求补交。

## 学习反馈

- 实际用时（排除中断，自报）：1h
- 难度与内容量：简单
- 仍需讲解的位置：无

- 结束时间：1012
