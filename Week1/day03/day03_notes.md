# Day 3 学习记录

## 开始前

1. `RequirementItem` 的两个字段分别是什么？
category和content
2. tuple 和 dataclass 都可以表示固定结构的数据，它们在读取方式上有什么区别？
tuple就是按顺序读取,dataclass则是按成员名访问读取,而且tuple是不可修改的
3. `frozen=True` 有什么作用？
不可修改 类似final?
4. `if item is None` 与 `if not item` 的含义是否完全相同？
不相同 is None就是单独判断是不是None ,而not则判断None | 0 | blank这些无意义的内容
## 完成后复盘

1. 实际用时：
没计时
2. pytest 最终结果：
通过
3. 今天独立完成了哪些生产代码和测试？
基本都是独立完成的
4. 今天遇到的第一个测试失败及修复方法：
test_ascii_colon_is_supported 这个case
for循环的对象搞错了 应该循环dict的keys 之前循环的是dict的values
5. tuple、list、dataclass 分别适合什么数据？
tuple适合存简单不变的数据 比如xy坐标
list则是比较灵活 存储过程中的变量集合
dataclass则可以存储一些数据单元 类似于javabean
6. 为什么不能只测试正常输入？
非预期输入时保证程序不崩溃
7. `None` 和空 list 分别表示什么？
None代表对象为空 相当于null
空list是有对象 但是对象里面没有内容 可以随时添加
8. 明天需要复习的内容：
目前还好 我觉得没啥需要复习的
9. 日语说明（3～5 句，介绍为什么增加数据校验和自动测试）：
バージョン２より、データ検証と単体テストケースを追加しました。
データ検証については、機能または受入条件がインプット資料に記載していない場合は、アウトプット資料に該当部分が漏れたことを明記し、責任者確認しやすくなる仕様です。
単体テストについては、今後機能改修、仕様追加する際に、現仕様に影響ないことが確認できるため、業務進めやすくなるとの仕様です。