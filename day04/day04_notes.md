# Day 4 学习记录

## 开始前

1. 一个 `.py` 文件为什么可以称为 module？

2. package 与普通文件夹的区别是什么？

3. `from .models import RequirementItem` 开头的 `.` 表示什么？

4. 为什么测试统一写 `from day04 import ...`，而不直接依赖每个内部文件？

## 完成后复盘

1. 实际用时：

2. pytest 最终结果：

3. 今天完成了哪些文件中的 TODO？

4. 第一个 import 错误是什么，如何定位和修复？

5. module、package 和 `__init__.py` 分别解决什么问题？

6. 为什么使用 `python -m day04.cli`，而不是直接执行 `day04/cli.py`？

7. 拆分文件后，pytest 为什么仍能验证业务行为没有改变？

8. 明天需要复习的内容：

9. 日语说明（3～5 句，介绍本次代码结构改善及其维护价值）：
