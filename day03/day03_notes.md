# Day 3 学习记录

## 开始前

1. `RequirementItem` 的两个字段分别是什么？

2. tuple 和 dataclass 都可以表示固定结构的数据，它们在读取方式上有什么区别？

3. `frozen=True` 有什么作用？

4. `if item is None` 与 `if not item` 的含义是否完全相同？

## 完成后复盘

1. 实际用时：

2. pytest 最终结果：

3. 今天独立完成了哪些生产代码和测试？

4. 今天遇到的第一个测试失败及修复方法：

5. tuple、list、dataclass 分别适合什么数据？

6. 为什么不能只测试正常输入？

7. `None` 和空 list 分别表示什么？

8. 明天需要复习的内容：

9. 日语说明（3～5 句，介绍为什么增加数据校验和自动测试）：
